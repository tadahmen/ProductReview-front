.form-fields {
  background-color: transparentize(#fff, 0.2);
  border-radius: 5px;
  margin-bottom: 2px;
  }

.submit-button {
  @extend .btn-default;
  background-color: transparentize(#fff, 0.2);
  padding: 10px;
  border-radius: 8px;
  border-style: outset;
  border-color: transparentize(white, 0.3);
}
